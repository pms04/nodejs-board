<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title><%= post.title %> - 상세 보기</title>
    <style>
        body { font-family: Arial, sans-serif; padding: 20px; }
        .container { max-width: 800px; margin: auto; }
        h1 { text-align: center; }
        .post-header { border-bottom: 2px solid #333; padding-bottom: 10px; margin-bottom: 20px; }
        .post-header h2 { margin: 0; }
        .post-meta { font-size: 0.9em; color: #555; margin-top: 5px; }
        .post-content { border: 1px solid #ddd; padding: 15px; min-height: 200px; white-space: pre-wrap; background-color: #f9f9f9; }
        .actions { margin-top: 20px; text-align: right; }
        .actions a, .actions button { 
            padding: 8px 15px; 
            margin-left: 5px; 
            text-decoration: none; 
            border: none; 
            border-radius: 4px; 
            cursor: pointer; 
            color: white;
            display: inline-block;
        }
        .actions a.edit { background-color: #007bff; }
        .actions button.delete { background-color: #dc3545; }
        .actions a.list { background-color: #6c757d; float: left; }
    </style>
</head>
<body>
    <div class="container">
        <h1>게시물 상세</h1>

        <div class="post-header">
            <h2><%= post.title %></h2>
            <div class="post-meta">
                작성자: <%= post.author %> | 
                작성일: <%= post.created_at.toISOString().split('T')[0] %> | 
                조회수: <%= post.views %>
            </div>
        </div>

        <div class="post-content">
            <%= post.content %>
        </div>

        <div class="actions">
            <a href="/" class="list">목록으로</a>
            
            <a href="/edit/<%= post.id %>" class="edit">수정</a>

            <form action="/delete/<%= post.id %>" method="POST" style="display: inline;">
                <button type="submit" class="delete" onclick="return confirm('정말로 삭제하시겠습니까?');">삭제</button>
            </form>
        </div>
    </div>
</body>
</html>